# Task

Implement the first reviewable slice of the Hostbox v1 roadmap: restore a truthful green build baseline, repair release/CLI packaging, fix the CLI's deployment API contract, and remove stale tracked build artifacts.

This task explicitly authorizes changes to CI/release workflow files and existing dependency versions/lockfiles within the stated scope. It does not authorize production deployment, schema changes, auth changes, or new product features.

# Task

Implement the Milestone 1 build-integrity slice: canonical path containment, deterministic exact-commit checkout, ephemeral GitHub authentication, detected-framework persistence, and terminal cancellation correctness.

The work is limited to the worker, path sanitizer, and narrowly required repository build-state/metadata operations. It must not change schema, public APIs, routing, handlers, notifications, workflows, or production infrastructure.

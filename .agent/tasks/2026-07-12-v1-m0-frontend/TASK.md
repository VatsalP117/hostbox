# Task

Implement the frontend and marketing baseline portion of Hostbox v1 Milestone 0.

Make the dashboard clean install, lint, type-check, and production build pass without weakening lint rules. Update compatible existing runtime dependencies to address fixable advisories. Make the marketing site clean install and production build pass, and add that verification to CI.

Only the assigned frontend, marketing dependency/configuration, CI test workflow, and task-report files are in scope. Do not commit; the milestone orchestrator owns integration and commit creation.

# Dynamic configuration snippets
# This directory is populated by the API server on deployment/domain changes
#
# Format: Each file in this directory is an importable Caddy snippet
# Example: 001-preview-myapp.conf
#
# The API server watches this directory and signals Caddy to reload
